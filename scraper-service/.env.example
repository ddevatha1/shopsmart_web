# Shared secret — must match the SCRAPER_SERVICE_TOKEN set in the main
# app's Vercel project env vars. Generate any long random string, e.g.:
#   openssl rand -hex 32
SCRAPER_SERVICE_TOKEN=

# Render sets this automatically in production; only needed for local dev.
PORT=3000
